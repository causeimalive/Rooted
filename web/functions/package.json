{
  "name": "rootedinchrist-functions",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "deploy": "npx firebase deploy --only functions",
    "emulate": "npx firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1",
    "raw-body": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
