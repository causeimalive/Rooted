import React from 'react'

export default function NetworkThreeScene(_props: any) {
  return (
    <div
      style={{
        width: '100%',
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: 'var(--text)',
        opacity: 0.7,
      }}
    >
      3D network view is currently unavailable.
    </div>
  )
}
